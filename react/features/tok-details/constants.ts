/**
 * The type of (redux) action which indicates that a get transcriptText with an
 * given message_id to be added or updated is received.
 *
 * {
 *      type: GET_TOK_MARKS,
 *      _tokMarksData: Object,
 * }
 */
export const GET_TOK_MARKS = 'GET_TOK_MARKS';
